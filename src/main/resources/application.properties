spring.application.name=project_Employees

spring.datasource.url=jdbc:mysql://localhost:3306/employees_directory?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=172641

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

# Shows the SQL queries in the console (useful for debugging your CRUD)
spring.jpa.show-sql=true

# Optional: Formats the SQL in the console to make it readable
spring.jpa.properties.hibernate.format_sql=true